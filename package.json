{
  "name": "bus-locations-socket",
  "version": "0.0.1",
  "description": "a socketio based app which can push bus locations to connected sockets",
  "dependencies": {
    "date-fns": "^2.9.0",
    "express": "^4.17.1",
    "log4js": "^6.1.1",
    "node-fetch": "^2.6.1",
    "socket.io": "^2.3.0"
  },
  "scripts": {
    "start": "node --experimental-modules --es-module-specifier-resolution=node src/server.js",
    "start:watch": "nodemon --experimental-modules --es-module-specifier-resolution=node src/server.js"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
